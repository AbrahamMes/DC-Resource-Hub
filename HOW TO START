### Backend

```NEW TERMINAL
cd .\backend
npm.cmd run dev
```

The backend runs at http://localhost:3001.

### Frontend

```NEW TERMINAL
cd .\Frontend\building-webapp
npm.cmd run dev
```

The frontend runs at http://localhost:5173. (LOAD INTO URL)